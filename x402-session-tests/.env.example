# Populated by `npm run gen` — do NOT commit real secrets.

# Facilitator service URL (start with: cd ../x402-session-facilitator && npm run dev)
FACILITATOR_URL=http://localhost:4021

# Network + RPC
NETWORK=base:sepolia
BASE_RPC_URL=https://sepolia.base.org

# Canonical Circle USDC on Base Sepolia (6 decimals)
USDC_ADDRESS=0x036CbD53842c5426634e7929541eC2318f3dCF7e

# User (payer) — signs the ERC20 approve tx. Populated by `npm run gen`.
# Must be manually funded with Base Sepolia ETH + USDC (see faucet links in generate-user.ts).
USER_PRIVATE_KEY=
USER_ADDRESS=

# Recipient (resource server wallet) — receives transferFrom funds. Populated by `npm run gen`.
RECIPIENT_PRIVATE_KEY=
RECIPIENT_ADDRESS=
